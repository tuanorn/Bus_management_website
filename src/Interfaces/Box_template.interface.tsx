export interface Box_template {
    title: string,
    text_value: JSX.Element,
    remark: string
}

export interface ProgressBar_template {
    progress: number,
    color1: string,
    color2: string,
    label: JSX.Element
}